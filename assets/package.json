{
  "private": true,
  "type": "module",
  "scripts": {
    "watch": "node build.js --watch",
    "build": "node build.js --deploy"
  },
  "dependencies": {
    "motion": "^10.17.0",
    "tailwindcss": "^3.4.6"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
